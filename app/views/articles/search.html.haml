.search-results
  - if @articles.present?
    %h4
      Tag search results for
      %span.search-param
        = params[:search]
    = render "shared/article_list"
  - else
    %h4
      No results found