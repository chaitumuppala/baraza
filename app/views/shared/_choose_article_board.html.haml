.article-board
  - if article
    %figure
      = link_to (image_tag article.cover_image.url), article_path(article.id)
      %figcaption
        = article.title
    %a.choose-article{href: "#", "data-reveal-id" => "chooseArticlesModal"} Choose article